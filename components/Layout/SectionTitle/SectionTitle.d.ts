import { default as React } from '../../../../node_modules/react';
export default SectionTitle;
declare function SectionTitle({ title, subtitle, align, variant }: {
    title: any;
    subtitle: any;
    align?: string;
    variant?: string;
}): React.JSX.Element;
declare namespace SectionTitle {
    namespace propTypes {
        let title: any;
        let subtitle: any;
        let align: any;
        let variant: any;
    }
}
