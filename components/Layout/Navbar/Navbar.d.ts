import { default as React } from '../../../../node_modules/react';
export default Navbar;
declare function Navbar({ logo, links, showSearch, showActions, }: {
    logo?: string;
    links?: any[];
    showSearch?: boolean;
    showActions?: boolean;
}): React.JSX.Element;
