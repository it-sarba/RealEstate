import { default as React } from '../../../../node_modules/react';
export function Alert({ type, message, description, onClose, }: {
    type?: string;
    message?: string;
    description?: string;
    onClose: any;
}): React.JSX.Element;
export default Alert;
