import { default as React } from '../../../../node_modules/react';
export function Snackbar({ open, message, type, duration, onClose, }: {
    open: any;
    message: any;
    type?: string;
    duration?: number;
    onClose: any;
}): React.JSX.Element;
export default Snackbar;
