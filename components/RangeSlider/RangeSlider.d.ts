import { default as React } from '../../../node_modules/react';
export default RangeSlider;
declare function RangeSlider({ min, max, step, value, onChange, label, }: {
    min?: number;
    max?: number;
    step?: number;
    value: any;
    onChange: any;
    label?: string;
}): React.JSX.Element;
