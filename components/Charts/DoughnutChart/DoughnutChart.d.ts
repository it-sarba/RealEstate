import { default as React } from '../../../../node_modules/react';
export function DoughnutChart({ data, dataKey, nameKey, title, innerRadius, outerRadius, }: {
    data: any;
    dataKey?: string;
    nameKey?: string;
    title: any;
    innerRadius?: number;
    outerRadius?: number;
}): React.JSX.Element;
export default DoughnutChart;
