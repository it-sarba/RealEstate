import { default as React } from '../../../../node_modules/react';
export function AreaChart({ data, xKey, areaKey, areaColor, strokeColor, title, }: {
    data: any;
    xKey: any;
    areaKey: any;
    areaColor?: string;
    strokeColor?: string;
    title: any;
}): React.JSX.Element;
export default AreaChart;
