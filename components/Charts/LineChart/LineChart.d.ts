import { default as React } from '../../../../node_modules/react';
export function LineChart({ data, xKey, lineKey, lineColor, title, strokeWidth, }: {
    data: any;
    xKey: any;
    lineKey: any;
    lineColor?: string;
    title: any;
    strokeWidth?: number;
}): React.JSX.Element;
export default LineChart;
