import { default as React } from '../../../../node_modules/react';
export function Sparkline({ data, dataKey, width, height, stroke, strokeWidth, showTooltip, }: {
    data: any;
    dataKey?: string;
    width?: number;
    height?: number;
    stroke?: string;
    strokeWidth?: number;
    showTooltip?: boolean;
}): React.JSX.Element;
export default Sparkline;
