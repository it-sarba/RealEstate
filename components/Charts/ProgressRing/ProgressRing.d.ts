import { default as React } from '../../../../node_modules/react';
export function ProgressRing({ radius, stroke, progress, color, label }: {
    radius?: number;
    stroke?: number;
    progress?: number;
    color?: string;
    label: any;
}): React.JSX.Element;
export default ProgressRing;
