import { default as React } from '../../../../node_modules/react';
export function PieChart({ data, dataKey, nameKey, title }: {
    data: any;
    dataKey?: string;
    nameKey?: string;
    title: any;
}): React.JSX.Element;
export default PieChart;
