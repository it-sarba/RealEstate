import { default as React } from '../../../../node_modules/react';
export function DataTable({ columns, data, rowsPerPage, rowKey, }: {
    columns?: any[];
    data?: any[];
    rowsPerPage?: number;
    rowKey?: string;
}): React.JSX.Element;
export default DataTable;
