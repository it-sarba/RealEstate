import { default as React } from '../../../../node_modules/react';
export function Badge({ children, color, variant, size, rounded, }: {
    children: any;
    color?: string;
    variant?: string;
    size?: string;
    rounded?: string;
}): React.JSX.Element;
export default Badge;
