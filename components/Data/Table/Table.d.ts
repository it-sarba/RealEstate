import { default as React } from '../../../../node_modules/react';
export function Table({ columns, data, rowKey, actions }: {
    columns?: any[];
    data?: any[];
    rowKey?: string;
    actions: any;
}): React.JSX.Element;
export default Table;
