import { default as React } from '../../../../node_modules/react';
export function Avatar({ src, alt, size, fallback, shape, }: {
    src: any;
    alt?: string;
    size?: string;
    fallback: any;
    shape?: string;
}): React.JSX.Element;
export default Avatar;
