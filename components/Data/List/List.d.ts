import { default as React } from '../../../../node_modules/react';
export function List({ items, ordered, icon, renderItem, }: {
    items?: any[];
    ordered?: boolean;
    icon?: any;
    renderItem: any;
}): React.JSX.Element;
export default List;
