import { default as React } from '../../../node_modules/react';
export default Rating;
declare function Rating({ max, value, onChange, size }: {
    max?: number;
    value?: number;
    onChange: any;
    size?: number;
}): React.JSX.Element;
