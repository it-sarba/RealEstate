import { default as React } from '../../../node_modules/react';
export default Stepper;
declare function Stepper({ steps, currentStep }: {
    steps?: any[];
    currentStep?: number;
}): React.JSX.Element;
