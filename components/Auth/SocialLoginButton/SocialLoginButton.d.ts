import { default as React } from '../../../../node_modules/react';
export function SocialLoginButton({ provider, label, onClick, disabled, }: {
    provider?: string;
    label?: string;
    onClick: any;
    disabled?: boolean;
}): React.JSX.Element;
export default SocialLoginButton;
