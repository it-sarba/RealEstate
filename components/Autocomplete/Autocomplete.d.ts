import { default as React } from '../../../node_modules/react';
export function Autocomplete({ options, onSelect, placeholder }: {
    options: any;
    onSelect: any;
    placeholder: any;
}): React.JSX.Element;
export namespace Autocomplete {
    namespace propTypes {
        let options: any;
        let onSelect: any;
        let placeholder: any;
    }
}
export default Autocomplete;
