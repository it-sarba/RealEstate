import { default as React } from '../../../../node_modules/react';
export function TagFilterBar({ tags, multiple, selected, onChange, clearable, }: {
    tags?: any[];
    multiple?: boolean;
    selected?: any[];
    onChange: any;
    clearable?: boolean;
}): React.JSX.Element;
export default TagFilterBar;
