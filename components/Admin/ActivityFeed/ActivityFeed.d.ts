import { default as React } from '../../../../node_modules/react';
export default ActivityFeed;
declare function ActivityFeed({ activities, variant }: {
    activities?: any[];
    variant?: string;
}): React.JSX.Element;
declare namespace ActivityFeed {
    namespace propTypes {
        let activities: any;
        let variant: any;
    }
}
