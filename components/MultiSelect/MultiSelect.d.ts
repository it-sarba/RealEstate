import { default as React } from '../../../node_modules/react';
export default MultiSelect;
declare function MultiSelect({ label, options, selected, onChange, placeholder }: {
    label: any;
    options: any;
    selected: any;
    onChange: any;
    placeholder: any;
}): React.JSX.Element;
declare namespace MultiSelect {
    namespace propTypes {
        let label: any;
        let options: any;
        let selected: any;
        let onChange: any;
        let placeholder: any;
    }
}
