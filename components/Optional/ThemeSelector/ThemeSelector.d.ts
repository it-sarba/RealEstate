import { default as React } from '../../../../node_modules/react';
export function ThemeSelector({ currentTheme, onChange }: {
    currentTheme?: string;
    onChange: any;
}): React.JSX.Element;
export default ThemeSelector;
