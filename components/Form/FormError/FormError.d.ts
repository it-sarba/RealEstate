import { default as React } from '../../../../node_modules/react';
export default FormError;
declare function FormError({ message }: {
    message: any;
}): React.JSX.Element;
declare namespace FormError {
    namespace propTypes {
        let message: any;
    }
}
