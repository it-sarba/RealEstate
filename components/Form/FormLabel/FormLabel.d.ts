import { default as React } from '../../../../node_modules/react';
export default FormLabel;
declare function FormLabel({ children, htmlFor, required }: {
    children: any;
    htmlFor: any;
    required: any;
}): React.JSX.Element;
declare namespace FormLabel {
    namespace propTypes {
        let children: any;
        let htmlFor: any;
        let required: any;
    }
}
